import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aprendendo-api-request',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
